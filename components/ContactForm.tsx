'use client'; import {useState} from 'react'; import {services} from '@/data/site';
export default function ContactForm(){const[s,setS]=useState('idle'),[err,setErr]=useState('');async function go(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setS('sending');const f=e.currentTarget;try{const r=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(f).entries()))});const j=await r.json();if(!r.ok)throw Error(j.error);f.reset();setS('success')}catch(e){setErr(e instanceof Error?e.message:'Unable to send');setS('error')}}if(s==='success')return <div className="success"><b>✓</b><h2>Thank you</h2><p>Your message has been submitted to Zelela Trading PLC. A member of our team will review your inquiry and respond as soon as possible.</p><button className="btn" onClick={()=>setS('idle')}>Send another message</button></div>;return <form className="form" onSubmit={go}><input className="trap" name="website" tabIndex={-1}/><div className="formgrid"><label>Full Name *<input required name="name" maxLength={120}/></label><label>Organization<input name="organization" maxLength={160}/></label><label>Email Address *<input required type="email" name="email"/></label><label>Phone Number<input name="phone"/></label><label className="full">Service Interested In *<select required name="service" defaultValue=""><option value="" disabled>Select a service</option>{services.map(x=><option key={x.slug}>{x.title}</option>)}<option>Training & Capacity Building</option><option>Other</option></select></label><label className="full">Subject *<input required name="subject"/></label><label className="full">Message *<textarea required name="message" rows={7} maxLength={5000}/></label></div>{s==='error'&&<p className="error">{err}</p>}<button className="btn primary" disabled={s==='sending'}>{s==='sending'?'Sending…':'Send Message'}</button><small>Your inquiry will be sent securely to zelelatradingplc@gmail.com.</small></form>}
