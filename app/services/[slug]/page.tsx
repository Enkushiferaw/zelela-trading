import Link from 'next/link'; import {notFound} from 'next/navigation'; import {services} from '@/data/site'; export function generateStaticParams(){return services.map(s=>({slug:s.slug}))} export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(x=>x.slug===slug);if(!s)notFound();return <main><section className="pageHero"><div className="wrap"><span className="eyebrow gold">{s.n} · Zelela Trading PLC</span><h1>{s.title}</h1><p>{s.short}</p></div></section><section className="section"><div className="wrap detail"><div><span className="eyebrow">Our capability</span><h2>Practical expertise for stronger outcomes.</h2><p>We support organizations with practical, evidence-informed advisory services designed around their specific institutional, enterprise, research or financial needs.</p><Link className="btn primary" href="/contact">Discuss this service ↗</Link></div><div><h3>Areas of support</h3><ul>{s.items.map(x=><li key={x}>✓ {x}</li>)}</ul></div></div></section></main>}
