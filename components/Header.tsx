'use client'; import Link from 'next/link'; import {useState} from 'react';
export default function Header(){const[open,setOpen]=useState(false);return <header className="header"><div className="wrap nav"><Link href="/" className="logo" onClick={()=>setOpen(false)}><span>Z</span><b>ZELELA<small>TRADING PLC</small></b></Link><button className="hamb" onClick={()=>setOpen(!open)} aria-label="Menu">☰</button><nav className={open?'open':''}>{[['/','Home'],['/about','About'],['/services','Services'],['/consultants','Consultants'],['/contact','Contact']].map(([h,t])=><Link key={h} href={h} onClick={()=>setOpen(false)}>{t}</Link>)}<Link className="navbtn" href="/contact">Request a Consultation</Link></nav></div></header>}
